demo-repo/
image.png
Backend/agents/__pycache__/
.claude/
docs/
CLAUDE.md
.worktrees/

# --- local scratch / artifacts (keep the working tree + pushes clean) ---
# screenshots & media (raw recordings/screenshots are not part of the repo)
image copy*.png
*.mp4
# tooling / brainstorm scratch
.superpowers/
# scaffold / demo projects (not part of the platform)
demo/
# local data backups (may contain PII — never commit)
backup-*/
backup-*.json
# misc scratch docs
aideas_top300_articles.html
BUGHUNT-PLAN.md
# python / build caches & test artifacts
__pycache__/
*.pyc
out-tsc/
**/playwright-report*/
**/test-results/
# terraform build artifacts (regenerated by plan/apply)
DevSecOps/Terraform/**/builds/
# windows alternate-data-stream junk
*:Zone.Identifier
