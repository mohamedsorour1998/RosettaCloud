<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>app.rosettacloud</groupId>
        <artifactId>rosettacloud-backend-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>lab-service</artifactId>
    <name>RosettaCloud Lab Service</name>
    <description>Per-lab Pod/Service/Istio VirtualService lifecycle + janitor + quota gate (replaces labs_backends.py)</description>

    <dependencies>
        <dependency>
            <groupId>app.rosettacloud</groupId>
            <artifactId>shared-lib</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>io.fabric8</groupId>
            <artifactId>kubernetes-client</artifactId>
        </dependency>

        <!-- Spring Cloud CircuitBreaker (Resilience4j backend) — Part B §B.3.
             Rolling-window failure-rate breaker + TimeLimiter + Micrometer meters on Boot 4. -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-circuitbreaker-resilience4j</artifactId>
        </dependency>
        <!-- Bulkhead is <optional> in the CircuitBreaker starter POM, so it is not pulled
             transitively; add it explicitly so the Resilience4j factory's ThreadPool/Bulkhead
             support autoconfigures (needed by Resilience4jBulkheadProvider; enables the per-id
             Bulkhead the target design calls for in §B.3.2). Version from resilience4j-bom. -->
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-bulkhead</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
